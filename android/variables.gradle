ext {
    minSdkVersion = 24
    // 36's download turned out to be incomplete (missing android.jar) —
    // 37 is what's actually fully present locally. Note: 37 is a preview
    // API level on this machine right now, fine for a debug test build,
    // but repair/reinstall a proper stable Platform 36 (or whatever's
    // current-stable by then) via Android Studio's SDK Manager before any
    // real release build.
    compileSdkVersion = 37
    targetSdkVersion = 37
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}